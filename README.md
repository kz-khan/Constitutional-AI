# Constitutional-AI

To run the project in bender:

1. submit job - "sbatch jobs/install.sh"
2. run using - "sbatch jobs/run.sh"
3. Before running see the config.py and keep one MODE true only(INIT_GENERATION_MODE,CRITIQUE_MODE,REVISION_MODE)